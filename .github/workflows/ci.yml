name: CI

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: uam_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d uam_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/uam_test
      BASE_URL: http://localhost:3000
      STEAM_API_KEY: ci-test-key
      SESSION_SECRET: ci-test-session-secret
      CREDENTIAL_ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      INTEGRATION_TESTS: "true"
      EMBEDDED_PG_TESTS: "true"
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: server/package-lock.json

      - run: npm ci
      - run: npm run db:migrate
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npx tsc --noEmit -p tsconfig.test.json
      - run: npm test

  desktop:
    runs-on: ubuntu-latest
    env:
      # Typechecking doesn't need the Electron binary itself.
      ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
    defaults:
      run:
        working-directory: desktop
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - run: npm ci
      - run: npm run typecheck

  windows-installer:
    runs-on: windows-latest
    env:
      # 1.0 ships unsigned; don't let electron-builder go looking for a cert.
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            server/package-lock.json
            desktop/package-lock.json

      - run: npm ci
        working-directory: server
      - run: npm ci
        working-directory: desktop
      - run: npm run dist
        working-directory: desktop

      - name: Install, launch, and uninstall the built installer
        shell: pwsh
        run: |
          $installer = (Get-ChildItem desktop/release/Unified-Achievement-Manager-Setup-*.exe).FullName
          ./desktop/scripts/smoke-test.ps1 -Installer $installer

      - uses: actions/upload-artifact@v4
        with:
          name: Unified-Achievement-Manager-Setup
          path: desktop/release/Unified-Achievement-Manager-Setup-*.exe
          retention-days: 14
